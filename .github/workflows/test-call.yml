# Temporary: reproduces the managed-repo stub call for debugging.
name: Test Reusable Call

on:
  workflow_dispatch: {}

jobs:
  triage:
    permissions:
      contents: write
      issues: write
      pull-requests: write
    uses: jirathip-k/agent-ops/.github/workflows/test-callee.yml@main
    with:
      target_repo: ${{ github.repository }}
      max_issues: 3
      auto_merge: false
    secrets:
      CLAUDE_CODE_OAUTH_TOKEN: ${{ secrets.CLAUDE_CODE_OAUTH_TOKEN }}
